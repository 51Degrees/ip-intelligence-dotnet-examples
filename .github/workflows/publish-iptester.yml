name: Publish IP Tester

on:
  workflow_dispatch:
    inputs:
      dryrun:
        type: boolean
        default: false

env:
  BASE_VERSION: '1.0'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - name: Create data directory and fetch assets
        run: |
          mkdir -p ip-intelligence-data
          wget -q -O ip-intelligence-data/51Degrees-EnterpriseIpiV41.ipi.gz '${{ secrets.IPI_DATA_FILE_URL }}'
          gunzip ip-intelligence-data/51Degrees-EnterpriseIpiV41.ipi.gz
      - name: Build
        run: |
          printf -v tag '%s-%(%y%m%dT%H%M)T' "$BASE_VERSION" -1
          docker build --platform=linux/amd64 --tag=51degrees/ipitester:{latest,"$tag"} .
      - name: Publish
        if: ${{ !inputs.dryrun }}
        env:
          USER: ${{ secrets.DOCKER_USERNAME }}
          PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
        run: docker login -u "$USER" --password-stdin <<<"$PASSWORD" && docker push -a 51degrees/ipitester